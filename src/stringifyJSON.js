// this is what you would do if you liked things to be easy:
// var stringifyJSON = JSON.stringify;

// but you don't so you're going to write it from scratch:

var stringifyJSON = function(obj) {
  var stringified = '';

  // if obj is equaled to an obj
  // if obj is an array
  // if obj is a number
  // if obj is a string
  // if obj is a boolean
  // if obj is null
  // if obj is undefined
  // if obj is function

};
